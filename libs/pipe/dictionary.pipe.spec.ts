/* tslint:disable:no-unused-variable */

import {
  async, inject
} from '@angular/core/testing';
import { Dictionary } from './dictionary.pipe';

describe('Pipe: dictionary', () => {
  it('create an instance', () => {
    let pipe = new Dictionary();
    expect(pipe).toBeTruthy();
  });
});
